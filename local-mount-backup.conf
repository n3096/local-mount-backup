# Central configuration for the backup suite
DEVICE_NAME="my-device"
MOUNT_POINT="/mnt/backup-usb"
DRIVE_UUID="0915a79b-24b6-4d65-8d79-1b6d1e08aca2"

# Derived variables
DEST_DIR="${MOUNT_POINT}/${DEVICE_NAME}/backup"
LOG_DIR="/srv/scripts/backup-${DEVICE_NAME}/logs"
LOCK_DIR="/srv/scripts/backup-${DEVICE_NAME}/lock"
BACKUP_SCRIPT_NAME="backup-${DEVICE_NAME}.sh"
LOCK_FILE="${LOCK_DIR}/${BACKUP_SCRIPT_NAME}.pid"